# Скрипты автоматизации развертывания и удаления виртуальных машин УТК-СЗИ

Скрипт install.sh выполняет следующие действия:
1. Проверяет наличие и в случае отсутствия создает виртуальные сети `intnet` и `extnet`;
2. Создает новый пул (по умолчанию с именем текущего пользователя);
3. Копирует из текущего каталога все имеющиеся образы виртуальных дисков в созданный пул и регистрирует их;
4. Создает виртуальные машины по всем xml-описаниям в текущем каталоге.

Запуск скрипта выполнять из терминала, запущенного в сеансе обучаемого(инструктора), т.к. пул создается в домашнем каталоге пользователя, запустившего скрипт.

Для создания пустых образов нужной емкости использовать команды:
qemu-img create -f qcow2 arm-abi.qcow2 16G
qemu-img create -f qcow2 srv-szi.qcow2 32G

TODO
Проверить работоспособность на Астре
Сделать скрипт uninstall.sh user_name, который корректно удаляет пулы и виртуальные машины пользователя user_name
Попробовать решить задачу манипулирования временем не изнутри виртуальной машины сервера СЗИ, а снуружи. Наиболее перспективный способ - использовать qemu-guest-agent

Если будет использоваться qemu-guest-agent, то добавить с конфиг ВМ канал:
<channel type="unix">
    <target type="virtio" name="org.qemu.guest_agent.0"/>
    <address type="virtio-serial" controller="0" bus="0" port="1"/>
</channel>
на ВМ установить и включить службу qemu-guest-agent.
После этих действий можно выполнять команды, например,
virsh -c qemu:///system qemu-agent-command "$DOMAIN_NAME" '{"execute":"guest-get-time"}'
но команды от root не запускаются, нужно разбираться как заставить

Можно совместить копирование qcow2 с установкой на них прав 660, правда выглядит очень длинным
sudo -u "$USER" find . -type f -name "*.qcow2" -exec sh -c 'cp -u "$1" "$2" && chmod 660 "$2/$(basename "$1")"' _ {} "$POOL_PATH" \;

NB! При запуске ВМ с дисками в домашнем каталоге в убунте нужно в файл /etc/libvirt/qemu.conf добавить параметр security_driver = "none". В Астре уточнить!
Если в Астре будут проблемы с правами доступа к дискам в пуле пользователя, то добавить к правам каталога SGID-бит (символ g+s или цифра 2 в правах)
sudo chown :kvm "$POOL_PATH"
chmod 2770 "$POOL_PATH"
теперь любые файлы, скопированные или созданные в этой папке автоматически получат группу kvm, а владелец останется вашим пользователем. 
